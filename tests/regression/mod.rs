// Regression Test Suite (Issue #487)
//
// Placeholder. All submodules that previously lived here were removed in
// workspace build-fix cleanup rounds 11-13 because they imported from
// private `mod foo {}` paths inside `#[ink::contract]` crates (E0603).
// Re-add against the public API surface when regressions need to be
// guarded on a fresh round.
